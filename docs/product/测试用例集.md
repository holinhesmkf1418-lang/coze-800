---
AIGC:
    Label: "1"
    ContentProducer: 001191110102MACQD9K64018705
    ProduceID: 2446840666792361_0/project_7648149359288172815-files/产品与测试/测试用例集.md
    ReservedCode1: ""
    ContentPropagator: 001191110102MACQD9K64028705
    PropagateID: 2446840666792361#1780746844645
    ReservedCode2: ""
---
# 搞定800词 · 第一轮测试用例集

## 1. 测试范围

本轮覆盖 MVP 核心链路：每日打卡、错题分析、随心测、资料导入基础校验。接口字段以后端 API 文档为准，本用例先定义业务验收口径。

## 2. 每日打卡

| 用例编号 | 场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TC-CHECKIN-001 | 正常打卡 | 用户已登录，当天未打卡，词库有数据 | 进入打卡页；查看今日词汇；点击打卡 | 返回成功；生成当天打卡记录；页面显示已完成 | P0 |
| TC-CHECKIN-002 | 重复打卡 | 用户已登录，当天已有打卡记录 | 再次进入打卡页；点击打卡 | 返回已打卡提示；不新增第二条有效记录 | P0 |
| TC-CHECKIN-003 | 跨天打卡 | 用户昨日已打卡，今天未打卡 | 第二天进入打卡页；点击打卡 | 今日可正常打卡；连续天数按规则更新 | P0 |
| TC-CHECKIN-004 | 未登录访问 | 用户未登录 | 打开打卡页 | 触发登录或返回登录提示，不展示个人打卡状态 | P1 |
| TC-CHECKIN-005 | 词库不足 | 词库为空或今日词汇不足 | 进入打卡页 | 返回明确错误或空状态，不崩溃 | P1 |

## 3. 错题分析

| 用例编号 | 场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TC-WRONG-001 | 收录错题 | 用户完成一次随心测且存在答错题 | 提交答案；进入错题本 | 答错词条出现在错题列表 | P0 |
| TC-WRONG-002 | 重复错题累加 | 同一用户已存在某词错题 | 再次测验答错同一词 | 错题不重复创建；错误次数 +1；最近错误时间更新 | P0 |
| TC-WRONG-003 | 统计准确性 | 用户错题覆盖多个分类 | 打开错题本统计面板 | 错题总数、分类数量、正确率计算准确 | P0 |
| TC-WRONG-004 | 分类汇总 | 错题包含分类 A、B、C | 切换分类筛选 | 列表只显示对应分类错题；分类计数正确 | P0 |
| TC-WRONG-005 | 空错题本 | 用户无错题 | 打开错题本 | 展示空状态和去测试入口 | P1 |

## 4. 随心测

| 用例编号 | 场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TC-TEST-001 | 随机抽题不重复 | 词库不少于 100 条 | 创建随心测 | 返回 100 道题；question_id 不重复 | P0 |
| TC-TEST-002 | 词库不足 100 条 | 词库少于 100 条 | 创建随心测 | 返回明确错误或按后端规则降级，不生成异常测试 | P0 |
| TC-TEST-003 | 正常提交成绩 | 已创建测试且未超时 | 作答部分正确；提交答案 | 返回总分、正确数、错误数、错题明细；测试记录落库 | P0 |
| TC-TEST-004 | 倒计时结束自动交卷 | 已创建测试，前端倒计时到 0 | 触发自动提交 | 后端按当前答案计算成绩，未答题计错或空答 | P0 |
| TC-TEST-005 | 超时后提交 | 当前时间晚于 expires_at | 提交答案 | 后端按超时规则处理，不接受伪造剩余时间 | P0 |
| TC-TEST-006 | 成绩计算 | 100 题中 73 题正确 | 提交答案 | 正确数 73，错误数 27，分数按约定公式返回 | P0 |
| TC-TEST-007 | 重复提交 | 同一 test_id 已提交 | 再次提交答案 | 返回已提交提示或原成绩，不重复写错题和测试记录 | P1 |

## 5. 资料导入

| 用例编号 | 场景 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|---|---|---|---|---|---|
| TC-IMPORT-001 | CSV 正常导入 | CSV 字段完整，800 条数据 | 调用导入接口 | 导入成功；返回成功条数；词库可查 | P0 |
| TC-IMPORT-002 | JSON 正常导入 | JSON 数组字段完整 | 调用导入接口 | 导入成功；字段正确落库 | P1 |
| TC-IMPORT-003 | 缺必填字段 | 部分行缺 term 或 definition | 调用导入接口 | 返回失败行号和原因；不产生脏数据 | P0 |
| TC-IMPORT-004 | 重复词条 | 文件中存在重复 term | 调用导入接口 | 返回重复提示；按后端策略跳过或更新 | P1 |
| TC-IMPORT-005 | 分类为空 | category 为空 | 调用导入接口 | 允许导入但归入默认分类，或返回明确校验结果 | P2 |

## 6. 第一轮验收建议

- 后端完成 API 文档后，将本测试用例中的“接口字段”补齐为具体请求和断言。
- 前端静态页验收优先看信息层级、倒计时与进度条位置、错题统计面板是否完整。
- 800 词 PDF 已上传，建议先转成 CSV 或 JSON 后再导入，避免直接解析 PDF 带来脏数据。

---

> 本内容由 Coze AI 生成，请遵循相关法律法规及《人工智能生成合成内容标识办法》使用与传播。
